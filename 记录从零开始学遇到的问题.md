# 1.vscode要用管理员身份运行 否则编译会失败

# 2.在终端编译运行的命令：
```
gcc xxx.c -o xxx.exe
.\xxx.exe

//如果要连接数学库这样写
gcc xxx.c -o xxx.exe -lm
//或者
gcc xxx.c-lm -o test.exe
//这样写报错
gcc xxx.c -o -lm test.exe
```

# 3.终端输出中文为乱码

### 解决方法一：
``` c
          #include <windows.h>
          int main(){
            SetConsoleOutputCP(65001);   为了让输出中文正常
            SetConsoleCP(65001）;          为了让输入中文正常
          }
```
          
### 解决方法二：

在终端输入
```
chcp 65001
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
```
### 解决方法三：

项目级配置runcode插件一键编译运行且中文显示正常 新建项目文件夹时新建子文件夹.vscode 在其里面新建settings.json复制
代码：
```
{
    "code-runner.runInTerminal": true,
    "code-runner.saveFileBeforeRun": true,
    "code-runner.clearPreviousOutput": true,
    "code-runner.executorMap": {
        "c": "cd $dir && gcc $fileName -o $fileNameWithoutExt -Wall && chcp 65001 && $dir$fileNameWithoutExt"
    }
}
```
# 4.对c文件进行调试
首先安装好gcc和gdb
### （1）终端命令编译
```
gcc -g xxx.c -o xxx.exe
gdb .\xxx.exe
```
回车后进入（gdb）开头的交互页面
断点要用终端命令设，用鼠标在代码里设的断点在终端不起效
```
       break main//在main函数入口设断点 
       break 10//在第十行设断点
       start//在main入口设断点并从头运行
       run//从头运行遇断点停下
       continue//继续运行直到遇到下一个断点或程序结束
       print a//打印变量a的值
       quit //结束调试
```
### （2）快捷方式调试
在.vscode文件夹中配置好tasks.json和launch.json以及下载c/c++扩展，
f5或者图形按钮一键调试 左边栏运行与调试处观察变量的值

# 5.关于scanf
### (1)%d %f %s会跳过前导空白
``` c
例：scanf（“%d%d”，&a，&b）；
输入：1 2
           1      2    
都是可以正确读入的
```
### （2）%c不会跳过空格，在%c前加上空格可以显式跳过空格
``` c
例：scanf（“%c”，&ch）；
输入：       a
只会读到空格
    scanf（“ %c”，&ch）；
输入：       a
可以读到a
```
### （3）空白字符表示匹配任意的空白字符
``` c
scanf（“%d %d ”，&a，&b）//因为第二个%d后面跟了一个空格 所以在没读到第三个非空白字符前没法输入
输入：    a        b        c//读出a b
```
### （4）普通字符必须原样照抄
``` c
例：scanf（“a+b=%d”，&c）
输入a+b=10
```
### （5）输入字符串不用& %S遇到空白就停
``` c
例：scanf（“%s”，s）；
输入：hello world
只能读到hello
```
### （6）scanf返回值是读取成功并赋值的变量个数
### （7）用fgets来得到带有空格的字符串
``` c
fgets（s，sizeof（s），stdin）
```
# 6.运算符优先级
```
* / %优先级高于 + —— 高于关系运算符高于赋值运算符
而在关系运算符中==和！=比其他关系运算符优先级低
连续的关系运算符从左至右运算
```
# 7.关于if语句的问题
## if（）后面跟分号相当于没有使用这个if逻辑
``` c
if（）；//相当于if（）后面跟了一句空语句，if块啥都没做就结束了
{
语句序列
}//这个语句序列无论是否满足if条件都会执行

```
# 8.关于switch语句问题
### （1）switch()中的控制表达式只能是整数型的结果
### （2）case的常量可以是常数或者计算常数的表达式
### （3）在case中执行语句如果没遇到break会一直往下顺序执行直到遇到break或switch结束

# 9.TIPS FOR LOOPS
### （1）有确定次数用for
### （2）必须做一次用do while
### （3）其它用while

# 10.跳出多层循环
``` c
\\break只能跳出当前循环 跳出多层循环需要接力break
\\程序目的 用一角两角和五角凑出x元 找到一个解即可
#include<stdio.h>
int main(){
    int x,one,two,five,exit=0;
    scanf("%d",&x);
    for(one=1;one<x*10;one++){
        for(two=1;two<x*10/2;two++){
            for(five=1;five<x*10/5;five++){
                if(one+two*2+five*5==x*10){
                    exit=1;
                    break;
                }
            }
           if(exit) break;//设置这个exit保证了内层break执行这个break才接力执行，防止内层循环完一轮没找到解后就在这里跳出了
        }
   if(exit) break;
 }
printf("%d个一角%d个两角%d个五角凑成 %d元",one,two,five,x);
}
```

# 11一道练习题
``` c
//正序分解整数
#include<stdio.h>
int main(){
    int x,t,quan=1;
    scanf("%d",&x);
    t=x;                //复制一份值用于第一个循环，一份值用于第二个循环
    while(t>9){
        t/=10;
       quan*=10;
    }                  //计算最高位的权 循环条件是t>9
   while(quan>0){
    printf("%d",x/quan);
    if(quan>1)
        printf(" ");
    x%=quan;
    quan/=10;
   }                      //正序分解输出 循环条件是quan>0
}
```

# 12.char和int的格式化输出
### 整型数据在计算机里都是用补码的形式表示
``` c
//char进入printf前会扩展为int，如果是有符号型char 则做符号扩展 若是unsigned char则会进行0扩展
#include<stdio.h>
int main(){
          char c=255;
          int i=255;
//进入printf前有符号char进行有符号扩展 1111 1111 1111...1111 1111 i 0000 0000.... 1111 1111
          printf("%d %d",c,i);
}
```
输出结果为 -1 255
``` c
#include<stdio.h>
int main(){
          char c=255;
          int i=255;
//进入printf前 signedchar进行有符号扩展 1111 1111.... 1111 1111 i 0000 0000... 1111 1111
          printf("%u %u",c,i);
//输出无符号数
}
```
输出 4294967295 255

``` c
#include<stdio.h>
int main(){
          unsigned char c=255;
          unsigned int i=255;
          //进入printf前 无符号char进行零拓展 c=0000 0000....11111111  i=0000 0000....1111 1111
          printf("%d %d",c,i);
}
```
输出 255 255

# 13.浮点运算的精度
``` c
#include<stdio.h>
int main(){
    float a,b,c;
    a=1.234f;
    b=1.234f;
    c=a+b;
    if(c==2.468){
        printf("相等\n");
    }
    else{
        printf("不相等 %.10f %f",c,c);
    }
}
```
输出：不相等 2.4679999352 2.468000
float是有精度的 1.234f存储的并不是精确值
f1==f2两个浮点数判断相等可能判断失败
应该这样写 fabs（f1-f2）<1e-8

# 14.变量作用域
``` c
#include <stdio.h>
void f(void);
int main() {
   int a=1;           //作用于main（）{}
  printf(" ");
   {
    int b=0;       //只作用于这个代码块{} 可以单独用一个{}来定义变量
    int a=2;         //内部代码块可以定义一个与外部代码块变量重名的变量并在内部代码块中覆盖它
    printf("%d",a);   //这里输出2而不是1 因为覆盖了
    printf(" ");
   }                //走出内部代码块了，b不存在了 而a是1而不是2，在内部代码块定义的变量虽然重名但只能作用于内部代码块
   f();          
}
void f(void){
    int c=3;
/*f()是不同于main的函数 两个函数变量空间是独立的 ，这里只有c作用，a不能在这里作用或者被访问，但可以在这里定义一个同名的a变量，但这个a和main中a是两个独立变量，不过要注意的是在同一个函数中不能定义重名的变量
   printf(" ");
}
```
```c
``` c
#include <stdio.h>
void f(void);
int main() {
   int a=1;          
  printf(" ");
   {
    int b=0;
    printf("%d",a);     //这里会输出1 外部代码块变量在内部代码块中仍然存在 ，这里我们并不是像上一例定义一个重名变量覆盖    
    a=2;
 /*与上一份代码不同的是 这次我们在内部代码块中并不是定义一个重名的变量进行覆盖 而是直接使用外部代码块的变量，这是合法的，外部代码块变量在内部代码块中仍然存在  */ 
    printf(" ");
   }       //此时a=2，在内部代码块中对a的修改被保留         
   f();          
}
void f(void){
    int c=3;
   printf(" ");
}
```
```
